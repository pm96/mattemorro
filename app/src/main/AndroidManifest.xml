<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.domene.tilfeldig.s305471mapp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity" android:parentActivityName=".StartActivity"></activity>
        <activity android:name=".PreferencesActivity" android:parentActivityName=".StartActivity"></activity>
        <activity android:name=".StatistikkActivity" android:parentActivityName=".StartActivity"></activity>
    </application>

</manifest>